{
  "name": "@thoth/mcp",
  "version": "0.1.0",
  "description": "MCP (Model Context Protocol) server wrapping the Thoth C++ pricing engine: pricing tools for vanillas / barriers / variance swaps, raw YAML book pricing and config-schema introspection over a running `thoth -server`.",
  "type": "module",
  "main": "dist/index.js",
  "bin": { "thoth-mcp": "dist/index.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@thoth/shared": "file:../shared",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
