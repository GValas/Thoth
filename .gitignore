# ── Engine (pricer/) build artifacts ──
/pricer/build/
/pricer/build-debug/
/pricer/build-gpu/
/pricer/build-san/
/pricer/build-cov/
/pricer/thoth
*.o
*.gcov
# stale root build dirs from before the monorepo move
/build/
/build-debug/
/build-gpu/
/build-san/
/build-cov/
/thoth

# Generated output
log.txt
*.out.yaml

# Engine TODO (local, untracked)
/pricer/TODO.md

# Claude local settings (runtime memory, worktrees, local settings stay local).
# Ignore the CONTENTS (not the dir itself) so the agent defs below can be re-included.
.claude/*
# …but share the reusable agent definitions with the team
!.claude/agents/
.claude/agents/*
!.claude/agents/*.md

# ── Web (web/) ──
web/**/node_modules/
web/**/dist/
web/bff/data/*.sqlite
web/**/.angular/
# TS incremental build artifact (caused a Docker build break when shipped in context)
web/**/*.tsbuildinfo
# redis dev data
redis-data/

# ── Secrets / local env (NEVER commit) ──
.env
